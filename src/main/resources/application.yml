spring:
  application:
    name: paralelismo-virthr
  threads:
    virtual:
      enabled: true
  # Mantém o AsyncTaskExecutor auto-configurado mesmo com o bean ExecutorService de VT.
  task:
    execution:
      mode: force
